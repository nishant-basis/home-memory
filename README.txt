HOME — ROOT PATH IMAGE FIX

GitHub repository layout:
- index.html
- all PNG image files in the repository root

The HTML therefore references images as:
  photo-01-first-look.png
not:
  assets/photo-01-first-look.png

Upload/replace index.html in the repository root. Keep the image files in the repository root exactly as they are.
